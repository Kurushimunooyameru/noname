#include <stdio.h>
int main(void)
{
    int n;
    printf("N = ");
    scanf("%i", &n);
    float x = 1;
    int i;
    for (i = 0; i <= n; ++i)
        x *= 1.0 + (float)i / 10;
    printf("произведение = %f \n ", x);
    return 0;
}
