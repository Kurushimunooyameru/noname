#include <stdio.h>
float Fact2(int n) 
{
    int x = 1;
    for (; n > 0; n -= 2) x *= n;
    return x;
}
int main(void)
{
    int i, n;
    for (i = 1; i <= 5; ++i) 
    {
        printf("N = ");
        scanf("%i", &n);
        printf("fact = %f\n", fact2(n));
    }
    return 0;
}
